# 手机与电脑课纲质量对齐测试

“同水准”指两端使用同一当前课程基线、同一结构与时间规则、同一事实与安全边界、同一排版程序，并分别通过同一质量门槛；不要求不同模型逐字生成相同文本。

## 每次发行必须通过

1. 当前课程清单恰好覆盖13份`outputs/catalog/*.md`；每项路径、标题、分类和规范化正文SHA256均与实际文件一致。
2. 手机核心规则包与三个方向包均由源文件自动生成；核心包加任一方向包少于45000字符，为客户需求和操作提示保留上下文余量。
3. 三个方向包完整覆盖13份当前课纲且不重复；当前课纲优先于历史母版。
4. 纯写稿精简ZIP醒目标注“仅Markdown”，不含Word模板、图片、Python脚本、PDF和手机聚合包；完整ZIP作为默认入口，保留模板、图片、脚本与运行依赖。
5. 手机包清单与两个ZIP清单记录相同的`basis_sha256`，证明采用同一组课程元数据与13份规范化正文；两个ZIP另记录相同的`layout_basis_sha256`，证明指向同一套模板、主排版脚本、视觉规范和图片。只有完整ZIP把这些排版资源实际嵌入包内。
6. 云端`render-result.json`记录源稿、模板、主脚本、Word、PDF和排版资源指纹；Word模板部件、Letter页面、图片关系与PDF逐页非空检查全部通过。
7. 全部自动测试、Skill结构校验和GitHub验证工作流通过。

## 真实前向测试

大版本更新后，用一个现有标准课中没有的需求分别开启两个全新会话：

- 电脑端：调用已安装的`bopai-kegang`。
- 手机端：只加载“手机核心规则包＋最相关方向包＋同一客户需求”。

两端不追加修稿提示，各自生成完整Markdown，再走同一个GitHub云端入口生成Word与PDF。推荐固定测试题为“两天制造业供应链AI办公与岗位助手实战课”，包含合同与公文、多表整理、岗位Skill、终稿对照、受控自动化与岗位AI工作台；WorkBuddy为主工具，禁止生产ERP直连、外传报价、GLM与iSlide。

## 共同硬门槛

- 客户版章节完整，多日区隔醒目，时间表只列上课且章节间隔正确。
- 每个教学模块按“关键词→讲解要点→演示→学员练习→产出物”展开。
- 指定对象、场景、工具、成果和安全边界全部出现；旧讲师数字、内部版本说明和未经核实的功能承诺不得出现。
- Markdown、Word与PDF的核心正文无遗漏；PDF无乱码、裁切、纯空白页或非必要低利用率页面。
- 甲方可读性、场景贴合、实操具体、课程递进、成果清晰五项各自评分；两端都达标，且总分差不超过5分。

## 证据

记录客户端、模型、Skill版本、共同`basis_sha256`、共同`layout_basis_sha256`、手机包SHA256、需求SHA256、两份Markdown、云端运行链接、Word哈希和PDF哈希。手机与电脑下载同一次运行的同一PDF；没有真实手机附件读取证据时，只能说明“同源包与模拟前向测试通过”，不能宣称已完成真机验证。
